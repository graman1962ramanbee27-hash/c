int lengthOfLastWord(char* s) {
    int count=0;
    int i;
    int length=0;
    i=strlen(s)-1;
    while(i>=0 && s[i]==' ')
    {
        i--;
    }
    while(i>=0 && s[i]
    !=' ')
    {
        length++;
        i--;
    }
            return length;    
    }
    
